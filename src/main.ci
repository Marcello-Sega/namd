mainmodule main {
  // main - defines most of the system wide simple messages

  message GroupInitMsg;
  message SlaveInitMsg;
  message LocalWorkMsg;

  mainchare main {
    entry main();
  };
  extern module WorkDistrib;
  extern module ReductionMgr;
  extern module PatchMgr;
  extern module ProxyMgr;
  extern module Node;
  extern module LdbCoordinator;
  extern module ComputeMgr;
  extern module CollectionMgr;
  extern module CollectionMaster;
  extern module BroadcastMgr;
}

